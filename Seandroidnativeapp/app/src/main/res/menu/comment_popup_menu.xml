<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/edit_comment"
        app:showAsAction="ifRoom|withText"
        android:title="@string/comment_edit_menu"
        android:visible="true"/>

    <item
        android:id="@+id/delete_comment"
        app:showAsAction="ifRoom|withText"
        android:title="@string/comment_delete_menu"
        android:visible="true"/>

    <item
        android:id="@+id/copy_comment"
        app:showAsAction="ifRoom|withText"
        android:title="@string/comment_copy_menu"
        android:visible="true"/>

    <item
        android:id="@+id/cancel_comment"
        app:showAsAction="ifRoom|withText"
        android:title="@string/comment_cancel_menu"
        android:visible="true"/>

</menu>
